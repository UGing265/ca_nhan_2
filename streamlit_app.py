# (Role: Team Lead + Frontend Lead + Frontend Logic) Main Streamlit application entry point.
# Integrates Monaco editor, manages st.session_state (workflow steps, session_id), calls ADK pipelines, and renders the UI layout.
